/** Total simulation minutes — match runs 0..95 */
export const SIM_TOTAL_MINUTES = 95;
/** Simulation clock tick interval (ms) */
export const SIM_TICK_INTERVAL_MS = 1_000;
/** Cooldown between scenario triggers (ms) */
export const SCENARIO_COOLDOWN_MS = 5_000;
/** Auto-approve delay for unattended playbooks (ms) */
export const AUTO_APPROVE_DELAY_MS = 60_000;
/** Delay between individual step executions in the playbook runner (ms) */
export const STEP_EXECUTION_DELAY_MS = 800;
/** Match kick-off hour (24h) */
export const SIM_START_HOUR = 19;
